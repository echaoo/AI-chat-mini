
• 核心瓶颈很明确：你现在是“每一轮都全量塞规则文档”。

  - 全量拼接发生在 src/modules/ai-companion/services/worldview.service.ts:95
  - 核心规则每次都读 3 个大文档 src/modules/ai-companion/services/worldview.service.ts:24
  - 每轮发送时都放进 system 消息 src/modules/ai-companion/services/conversation.service.ts:344、src/modules/ai-companion/prompts/prompt-builder.ts:46
  - 目前规则文档体积约 83KB（仅 md 原文），这还没算角色设定和历史消息

  最有效的优化顺序：

  1. 先做“压缩规则包”（立刻降本）

  - 保留完整文档做知识库，但对话只注入 rule-card（1-2k tokens）
  - WorldviewService 默认加载 compact，仅在特定场景加载 full
  - 预计可降 50%-80% 的 prompt tokens

  2. 把“固定 20 条历史”改成“token 预算截断”

  - 现在 take: 20 在 src/modules/ai-companion/services/conversation.service.ts:337 附近
  - 改成按 token 上限保留最近消息（例如 4k-6k），超出就摘要
  - 长对话场景再降 20%-40%

  3. 规则按需加载（触发式）

  - 日常闲聊只用核心短规 + 物种短规
  - 用户提到“能力边界/世界观追问/冲突判定”再补充对应长规则片段
  - 能继续降成本并减少模型负担

  4. 提升缓存命中率

  - 你已经记录了 cachedTokens，很好
  - 再把系统前缀做成稳定字符串（版本号 + hash），减少前缀抖动

  5. 关闭“完整消息 debug 日志”

  - 现在会打印整包消息 src/modules/ai-companion/services/conversation.service.ts:369
  - 改成只打长度和token统计，减少噪音与潜在风险

  如果你要，我可以直接给你做第一版落地改造（compact规则包 + token预算历史截断），这两个改完通常就能看到最明显的成本下降。